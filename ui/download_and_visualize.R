 tabPanel("Download and Visualize",
    dashboardPage(
      dashboardHeader(title = "dl and vis"),
      dashboardSidebar(),
      dashboardBody(

      ) #dashboardBody end

    ) #dash end
    ) #tabPanel end